Adds basic javascript functionality to identify elements in the dom.

Defines variables for each number element in the dom.
Adds function to display users number sequence in the display based on clicks.

